[phases.setup]
nixPkgs = ["python311"]

[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "python3 download_fonts.py"
]

[start]
cmd = "gunicorn webhook:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
